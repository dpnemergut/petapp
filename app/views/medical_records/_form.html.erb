<%= form_for(@medical_record) do |f| %>
  <% if @medical_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medical_record.errors.count, "error") %> prohibited this medical_record from being saved:</h2>

      <ul>
      <% @medical_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pet_id %><br />
    <%= f.number_field :pet_id %>
  </div>
  <div class="field">
    <%= f.label :rabies %><br />
    <%= f.date_select :rabies %>
  </div>
  <div class="field">
    <%= f.label :distemper %><br />
    <%= f.date_select :distemper %>
  </div>
  <div class="field">
    <%= f.label :conditions %><br />
    <%= f.text_area :conditions %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
